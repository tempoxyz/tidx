name: Auto Assign PR

on:
  pull_request:
    types: [opened, reopened]

permissions:
  issues: write
  pull-requests: write

jobs:
  assign:
    uses: tempoxyz/ci/.github/workflows/auto-assign-pr.yml@main
