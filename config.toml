# AK47 Indexer Configuration

# Use timescaledb for docker, localhost for local dev
database_url = "postgres://ak47:ak47@timescaledb:5432/ak47"

# HTTP API settings
port = 8080
bind = "0.0.0.0"

# Prometheus metrics
metrics_port = 9090

# Chains to index
[[chains]]
name = "moderato"
chain_id = 42429
rpc_url = "https://rpc.moderato.tempo.xyz"
backfill = true
batch_size = 100

# Add more chains:
# [[chains]]
# name = "presto"
# chain_id = 4217
# rpc_url = "https://rpc.presto.tempo.xyz"
